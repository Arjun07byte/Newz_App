<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NewzApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/myWhite</item>
        <item name="colorPrimaryVariant">@color/myPrimary</item>
        <item name="colorOnPrimary">@color/myPrimary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/myBlack</item>
        <item name="colorSecondaryVariant">@color/myPrimary</item>
        <item name="colorOnSecondary">@color/myPrimary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@color/myWhite</item>
    </style>
</resources>